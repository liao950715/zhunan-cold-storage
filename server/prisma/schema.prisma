// Stage 0：僅設定 datasource／generator。資料模型於 Stage 1 依 docs/DATABASE_DESIGN.md 建立。
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
